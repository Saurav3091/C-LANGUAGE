#include <stdio.h>
#include <conio.h>

void main()
{

    inntf("(a-b)2\n");
    print a = 5, b = 2;
    pritf("a2 - 2ab + b2\n");
    printf("%d - %d + %d\n", a * a, 2 * a * b, b * b);
    printf("%d", (a * a) - (2 * a * b) + (b * b));
}